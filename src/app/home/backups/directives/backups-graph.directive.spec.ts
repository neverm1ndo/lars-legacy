import { BackupsGraphDirective } from './backups-graph.directive';

describe('BackupsGraphDirective', () => {
  it('should create an instance', () => {
    const directive = new BackupsGraphDirective();
    expect(directive).toBeTruthy();
  });
});
